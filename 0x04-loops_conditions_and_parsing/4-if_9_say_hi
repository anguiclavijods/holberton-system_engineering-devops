#!/usr/bin/env bash
#print message, validate loop while, if
iter=0
while [ $iter -le 9 ]
do
    echo "Holberton School"
    iter=$((iter + 1))
    if [ $iter -eq 9 ];
        then
        echo "Hi"
    fi
done
